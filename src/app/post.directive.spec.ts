import { PostDirective } from './post.directive';

describe('PostDirective', () => {
  it('should create an instance', () => {
    const directive = new PostDirective();
    expect(directive).toBeTruthy();
  });
});
